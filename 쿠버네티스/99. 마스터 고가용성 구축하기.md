참고사이트

```
https://danawalab.github.io/kubernetes/2020/01/28/kubernetes-ha-cluster.html
```

---

호스트네임 변경

```
hostnamectl set-hostname node2
```



```
apt update
apt install docker.io -y

apt-get -y install \
    apt-transport-https \
    ca-certificates \
    curl \
    gnupg-agent \
    software-properties-common
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | apt-key add -
add-apt-repository "deb [arch=arm64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
apt-get -y update
apt-get -y install docker-ce docker-ce-cli containerd.io

# Cgroup를 systemd로 설정
cat > /etc/docker/daemon.json <<EOF
{
  "exec-opts": ["native.cgroupdriver=systemd"],
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "100m"
  },
  "storage-driver": "overlay2"
}
EOF

mkdir -p /etc/systemd/system/docker.service.d
systemctl daemon-reload
systemctl restart docker
sudo systemctl enable docker

cat <<EOF | tee /etc/modules-load.d/k8s.conf
br_netfilter
EOF

cat <<EOF | tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-ip6tables = 1
net.bridge.bridge-nf-call-iptables = 1
EOF

apt-get update && apt-get install -y apt-transport-https curl

curl -fsSLo /usr/share/keyrings/kubernetes-archive-keyring.gpg https://packages.cloud.google.com/apt/doc/apt-key.gpg

echo "deb [signed-by=/usr/share/keyrings/kubernetes-archive-keyring.gpg] https://apt.kubernetes.io/ kubernetes-xenial main" | sudo tee /etc/apt/sources.list.d/kubernetes.list

sudo swapoff -a

apt-get update
apt-get install -y kubelet kubeadm kubectl
apt-mark hold kubelet kubeadm kubectl

sudo snap install helm --classic
```



```
sudo kubeadm init --control-plane-endpoint "192.168.150.179:6443" \
                --upload-certs \
                --pod-network-cidr "10.244.0.0/16"
```



```
kubectl apply -f "https://cloud.weave.works/k8s/net?k8s-version=$(kubectl version | base64 | tr -d '\n')"
```



```
You can now join any number of the control-plane node running the following command on each as root:

  kubeadm join 192.168.150.179:6443 --token ti7ryp.ncy32pfelk5f97qw \
        --discovery-token-ca-cert-hash sha256:0c51c36953b6c5537d89a45565622ba95f6b7df4699694710009d23a08f73e46 \
        --control-plane --certificate-key 3efca519820be1f06e587abf182e3edf3a110a852d9b9dcff82ad671dda53be9
```

```
kubeadm join 192.168.150.179:6443 --token ti7ryp.ncy32pfelk5f97qw \
        --discovery-token-ca-cert-hash sha256:0c51c36953b6c5537d89a45565622ba95f6b7df4699694710009d23a08f73e46
```

```
kubeadm join 10.10.0.14:6443 --token o36uz9.j4pxjz6ue908vim3 \
        --discovery-token-ca-cert-hash sha256:a1b16d90d89e9337b6fe4e5e2bc45473e6f21cbf78c98064a07079d056f15734
```

---

```
sudo kubeadm init --control-plane-endpoint "101.79.1.134:6443" \
                --upload-certs \
                --pod-network-cidr "10.244.0.0/16"
```

```
  kubeadm join 101.79.1.134:6443 --token dgwlwv.lpcz3kfstgh4du86 \
        --discovery-token-ca-cert-hash sha256:a1deaaf0266184958db01421b7aed98db6b294cd5ba8bdc4bcf1e5d7b43e5f24 \
        --control-plane --certificate-key eb0e874186cc57cd36e6d6896fc4d6e96ce75526d3e2995fe2b1a97833aa2e7e
```

```
kubeadm join 101.79.1.134:6443 --token dgwlwv.lpcz3kfstgh4du86 \
        --discovery-token-ca-cert-hash sha256:a1deaaf0266184958db01421b7aed98db6b294cd5ba8bdc4bcf1e5d7b43e5f24
```

